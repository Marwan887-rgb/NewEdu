# 🔧 دليل التطوير المحلي

## 🎯 طريقتان للتطوير المحلي:

---

## الطريقة 1: استخدام netlify dev (موصى به) ⭐

### **المميزات:**
```
✅ يحاكي Netlify تماماً
✅ يشغل Functions محلياً
✅ يقرأ من .env.local تلقائياً
✅ لا حاجة لحفظ التوكن في المتصفح كل مرة
✅ احترافي
```

### **الخطوات:**

#### 1. ضع التوكن في `.env.local`:

```bash
# افتح الملف: .env.local
# استبدل "ضع_توكنك_هنا" بالتوكن الحقيقي

TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

#### 2. شغّل netlify dev:

```bash
cd "c:/Users/mrn88/OneDrive/المستندات/EduActive"

netlify dev
```

#### 3. افتح المتصفح:

```
http://localhost:8888
```

#### 4. جرّب تسجيل الدخول:
```
✅ أدخل رقم جوال
✅ اضغط "إرسال رمز"
✅ يجب أن يصل OTP للتلجرام
✅ أدخل الكود → دخول!
```

---

## الطريقة 2: فتح index.html مباشرة (بسيطة)

### **المميزات:**
```
✅ سهلة جداً
✅ لا تحتاج تثبيت شيء
✅ مناسبة للاختبار السريع
```

### **العيوب:**
```
❌ تحتاج حفظ التوكن في المتصفح كل مرة
❌ إذا مسحت بيانات المتصفح → يختفي التوكن
```

### **الخطوات:**

#### 1. احفظ التوكن (مرة واحدة):

```
1. افتح telegram-setup.html
2. ألصق التوكن
3. احفظ
```

#### 2. افتح index.html:

```
افتح index.html مباشرة
أو استخدم Live Server
```

#### 3. جرّب:
```
✅ التوكن محفوظ في localStorage
✅ يعمل حتى تمسح بيانات المتصفح
```

---

## 🔒 الأمان:

### ملف `.env.local`:
```
✅ محمي في .gitignore
✅ لن يتم رفعه على GitHub
✅ لن يتم رفعه على Netlify
✅ موجود على جهازك فقط
✅ آمن للتطوير
```

---

## 📊 المقارنة:

| الميزة | netlify dev | فتح مباشر |
|--------|-------------|-----------|
| سهولة البدء | متوسطة | سهل جداً |
| يحاكي الإنتاج | ✅ نعم | ❌ لا |
| التوكن ثابت | ✅ نعم (.env) | ❌ لا (localStorage) |
| يشغل Functions | ✅ نعم | ✅ نعم (بشرط) |
| للاختبار السريع | ⚠️ بطيء قليلاً | ✅ سريع |
| احترافي | ✅✅✅ | ⚠️ للاختبار فقط |

---

## 💡 التوصية:

### **للتطوير اليومي:**
```bash
netlify dev
```

### **للاختبار السريع:**
```
افتح index.html مباشرة
+ telegram-setup.html (لحفظ التوكن)
```

---

## 🚀 أوامر مفيدة:

```bash
# تشغيل محلي
netlify dev

# تشغيل على بورت محدد
netlify dev --port 3000

# عرض Environment Variables
netlify env:list

# رفع على Netlify
netlify deploy --prod
```

---

## ❓ أسئلة شائعة:

### س: هل `.env.local` آمن؟
**ج:** نعم، محمي في `.gitignore` ولن يتم رفعه

### س: هل أحتاج netlify-cli مثبت؟
**ج:** نعم، لاستخدام `netlify dev`
```bash
npm install -g netlify-cli
```

### س: هل يمكن استخدام .env بدلاً من .env.local؟
**ج:** نعم، لكن .env.local أوضح أنه للتطوير المحلي فقط

### س: أين أضع التوكن للإنتاج (Netlify)؟
**ج:** في Netlify Dashboard → Environment Variables

---

## 📝 ملاحظات:

1. **لا تشارك `.env.local` مع أحد** ⚠️
2. **لا تدفعه على GitHub** ⚠️
3. **اعمل نسخة احتياطية منه** ✅
4. **استخدم توكن مختلف للإنتاج** ✅

---

**الآن يمكنك التطوير بدون حفظ التوكن كل مرة!** ✅
