# 🔍 فحص أرقام الجوال في قاعدة البيانات

## ❌ المشكلة:
"رقم الجوال غير مسجل في النظام" رغم أن الطالب موجود

## 🔧 الحل المطبق:

### 1. تحسين دالة normalizeMobile
- ✅ الآن تدعم جميع الصيغ:
  - `+966501234567` → `966501234567`
  - `00966501234567` → `966501234567`
  - `0501234567` → `966501234567`
  - `501234567` → `966501234567`
  - `966501234567` → `966501234567`

### 2. البحث المتعدد
- ✅ يبحث بالصيغة المطبعة
- ✅ إذا لم يجد، يبحث بدون 966
- ✅ إذا لم يجد، يبحث بصيغة 05

### 3. رسائل تشخيصية
- ✅ يعرض الرقم المدخل
- ✅ يعرض الرقم بعد التطبيع
- ✅ رسالة واضحة في Console

---

## 🧪 اختبار:

### 1. افتح Console (F12)
### 2. جرب إدخال رقم جوال
### 3. راقب Console للأخطاء:
```javascript
❌ [OTP] خطأ في البحث عن الطالب: {
  mobileSearched: "966501234567",
  error: {...},
  rawInput: "0501234567"
}
```

---

## 🔍 فحص قاعدة البيانات:

### تحقق من صيغة الرقم في قاعدة البيانات:

```sql
-- في Supabase SQL Editor:
SELECT mobile, name FROM students LIMIT 10;
```

### الصيغ المتوقعة:
- ✅ `966501234567` (موصى به)
- ✅ `0501234567`
- ✅ `501234567`

---

## 🛠️ إصلاح قاعدة البيانات (إذا لزم):

### إذا كانت الأرقام بصيغة مختلفة:

```sql
-- تحديث جميع الأرقام إلى صيغة موحدة (966):
UPDATE students 
SET mobile = '966' || SUBSTRING(mobile FROM 2)
WHERE mobile LIKE '05%';

-- أو
UPDATE students 
SET mobile = '966' || mobile
WHERE mobile LIKE '5%' AND LENGTH(mobile) = 9;
```

---

## ✅ النتيجة:

بعد التحديث:
- ✅ يدعم جميع صيغ الأرقام
- ✅ يبحث بصيغ متعددة
- ✅ رسائل تشخيصية واضحة
- ✅ يعمل مع أي صيغة في قاعدة البيانات

---

**جرب الآن وأخبرني بالنتيجة!**

