# 🧪 دليل اختبار PWA - بالتفصيل

## 📋 الخطوات خطوة بخطوة

---

## 1️⃣ فتح التطبيق في Chrome

### الخطوات:
1. **افتح Google Chrome** على جهاز الكمبيوتر
2. **اذهب إلى التطبيق**:
   - إذا كان محلياً: `http://localhost:8888` أو `file:///C:/path/to/index.html`
   - إذا كان على Netlify: `https://your-app.netlify.app`
3. **افتح الصفحة الرئيسية** (`index.html`)

### ✅ ما يجب أن تراه:
- الصفحة الرئيسية للتطبيق تعمل بشكل طبيعي
- لا توجد أخطاء في Console (افتح F12 للتأكد)

---

## 2️⃣ فتح DevTools

### الخطوات:
1. **اضغط `F12`** على لوحة المفاتيح
   - أو: **اضغط بزر الماوس الأيمن** على الصفحة → **Inspect** (فحص)
   - أو: **من القائمة**: View → Developer → Developer Tools

### ما ستراه:
```
┌─────────────────────────────────────────┐
│ Elements  Console  Sources  Network    │
│ Application  Performance  Memory  ...   │
└─────────────────────────────────────────┘
```

### ✅ النتيجة:
- DevTools مفتوح في أسفل أو جانب الشاشة

---

## 3️⃣ فتح تبويب Application

### الخطوات:
1. **في DevTools**، ابحث عن تبويب **"Application"**
2. **اضغط عليه**
3. في **الجانب الأيسر**، ستجد قائمة منسدلة

### ما ستراه:
```
Application
├── Manifest
├── Service Workers  ← هنا
├── Storage
├── Cache Storage
└── ...
```

### ✅ النتيجة:
- تبويب Application مفتوح
- القائمة الجانبية مرئية

---

## 4️⃣ التحقق من Service Workers

### الخطوات التفصيلية:

#### أ. ابحث عن "Service Workers"
1. في القائمة الجانبية اليسرى، **ابحث عن "Service Workers"**
2. **اضغط عليه**

#### ب. ما ستراه:
```
┌────────────────────────────────────────────────┐
│ Service Workers                                │
├────────────────────────────────────────────────┤
│                                                │
│  Source: /sw.js                               │
│  Status: ● activated and is running           │
│  Scope: /                                     │
│                                                │
│  [Update] [Unregister]                        │
└────────────────────────────────────────────────┘
```

### ✅ علامات النجاح:
- ✅ **Status**: "activated and is running" (أخضر)
  - يعني: Service Worker يعمل بنجاح
- ✅ **Source**: `/sw.js`
  - يعني: الملف موجود ويتم تحميله
- ✅ **Scope**: `/`
  - يعني: يغطي جميع الصفحات

### ❌ إذا رأيت خطأ:
- **Status**: "redundant" أو "error"
  - **الحل**: تحقق من أن ملف `sw.js` موجود في المجلد الرئيسي
  - **الحل**: تحقق من Console للأخطاء (F12 → Console)

---

## 5️⃣ التحقق من Manifest

### الخطوات التفصيلية:

#### أ. ابحث عن "Manifest"
1. في القائمة الجانبية اليسرى، **ابحث عن "Manifest"**
2. **اضغط عليه**

#### ب. ما ستراه:
```
┌────────────────────────────────────────────────┐
│ Manifest                                       │
├────────────────────────────────────────────────┤
│                                                │
│  App name: بوابة التعلم الذكية - EduActive   │
│  Short name: EduActive                         │
│  Start URL: /                                  │
│  Display: standalone                           │
│  Theme color: #7c3aed                          │
│                                                │
│  Icons:                                        │
│  ├─ icon-192.png (192x192) ✅                 │
│  └─ icon-512.png (512x512) ✅                 │
│                                                │
│  [Add to homescreen]                          │
└────────────────────────────────────────────────┘
```

### ✅ علامات النجاح:
- ✅ **App name**: يظهر اسم التطبيق
- ✅ **Icons**: تظهر الأيقونات (192x192 و 512x512)
  - إذا رأيت ❌ بجانب الأيقونات: الملفات غير موجودة
- ✅ **Theme color**: #7c3aed (بنفسجي)
- ✅ **Display**: standalone (يعمل كتطبيق مستقل)

### ❌ إذا رأيت خطأ:
- **Error**: "Manifest file not found"
  - **الحل**: تحقق من أن ملف `manifest.json` موجود في المجلد الرئيسي
  - **الحل**: تحقق من أن الرابط في `index.html` صحيح: `<link rel="manifest" href="/manifest.json">`

---

## 6️⃣ اختبار وضع Offline

### الخطوات التفصيلية:

#### أ. تفعيل وضع Offline
1. في **DevTools**، اذهب إلى تبويب **"Network"**
2. في أعلى تبويب Network، ستجد **checkbox** اسمه **"Offline"**
3. **اضغط على checkbox** لتفعيل وضع Offline

### ما ستراه:
```
┌────────────────────────────────────────────────┐
│ Network                                        │
├────────────────────────────────────────────────┤
│  [☑] Offline  ← اضغط هنا                      │
│                                                │
│  (أيقونة صغيرة تشير إلى عدم وجود إنترنت)     │
└────────────────────────────────────────────────┘
```

#### ب. اختبار التطبيق
1. **عد إلى الصفحة الرئيسية** (Tab الخاص بالتطبيق)
2. **حاول التنقل** بين الصفحات
3. **حاول فتح الدروس** أو المحتوى

### ✅ علامات النجاح:
- ✅ **الصفحة الرئيسية** تظهر (من التخزين المؤقت)
- ✅ **الملفات الأساسية** تعمل (CSS, JS)
- ✅ **التصميم** يظهر بشكل صحيح
- ✅ **لا توجد أخطاء** في Console

### ❌ ما لن يعمل (وهذا طبيعي):
- ❌ **طلبات Supabase API**: تحتاج إنترنت دائماً
- ❌ **طلبات DeepSeek API**: تحتاج إنترنت دائماً
- ❌ **المحتوى الجديد**: غير مخزن في Cache

#### ج. إعادة تفعيل الإنترنت
1. **عد إلى تبويب Network**
2. **ألغِ تفعيل checkbox "Offline"**
3. **تأكد من أن الإنترنت عاد**

---

## 🎯 اختبارات إضافية (اختياري)

### 1. اختبار التثبيت

#### على Android Chrome:
1. افتح التطبيق في Chrome
2. سيظهر **إشعار** في أسفل الشاشة: "إضافة إلى الشاشة الرئيسية"
3. **اضغط على الإشعار**
4. سيتم تثبيت التطبيق

#### على iOS Safari:
1. افتح التطبيق في Safari
2. اضغط **زر المشاركة** (Share) في الأسفل
3. اختر **"إضافة إلى الشاشة الرئيسية"**
4. سيتم تثبيت التطبيق

#### على Desktop Chrome:
1. افتح التطبيق في Chrome
2. في شريط العنوان، ستجد **أيقونة +** أو **أيقونة تثبيت**
3. **اضغط عليها**
4. سيتم تثبيت التطبيق

### 2. اختبار Cache

#### في DevTools:
1. اذهب إلى **Application → Cache Storage**
2. ستجد **"eduactive-v1"** و **"eduactive-runtime-v1"**
3. **اضغط على أي منهما** لرؤية الملفات المخزنة

### 3. اختبار Service Worker Update

#### في DevTools:
1. اذهب إلى **Application → Service Workers**
2. **عدّل ملف `sw.js`** (غير رقم الإصدار)
3. **اضغط على "Update"** في Service Workers
4. **أعد تحميل الصفحة** (F5)
5. يجب أن يتم تحديث Service Worker تلقائياً

---

## 📊 ملخص النتائج المتوقعة

### ✅ إذا كان كل شيء يعمل:
```
✅ Service Worker: activated and running
✅ Manifest: loaded successfully
✅ Icons: 2 icons found
✅ Offline: Basic functionality works
✅ Install: Prompt available
```

### ❌ إذا كان هناك مشاكل:
```
❌ Service Worker: Error loading
   → الحل: تحقق من ملف sw.js

❌ Manifest: File not found
   → الحل: تحقق من ملف manifest.json

❌ Icons: Not found
   → الحل: أنشئ الأيقونات باستخدام generate-icons.html

❌ Offline: Not working
   → الحل: تحقق من Service Worker
```

---

## 🆘 حل المشاكل الشائعة

### المشكلة 1: Service Worker لا يعمل
**الأسباب المحتملة:**
- الملف غير موجود
- خطأ في الكود
- HTTPS مطلوب (للموقع المنشور)

**الحل:**
1. تحقق من Console للأخطاء (F12 → Console)
2. تأكد من وجود ملف `sw.js` في المجلد الرئيسي
3. إذا كان محلياً: استخدم `http://localhost` (لا `file://`)

### المشكلة 2: Manifest لا يعمل
**الأسباب المحتملة:**
- الملف غير موجود
- خطأ في JSON
- الرابط غير صحيح

**الحل:**
1. تحقق من وجود `manifest.json`
2. تحقق من صحة JSON (استخدم JSON validator)
3. تحقق من الرابط في `index.html`

### المشكلة 3: الأيقونات لا تظهر
**الأسباب المحتملة:**
- الملفات غير موجودة
- الأسماء غير صحيحة
- المسارات غير صحيحة

**الحل:**
1. استخدم `generate-icons.html` لإنشاء الأيقونات
2. تأكد من الأسماء: `icon-192.png` و `icon-512.png`
3. ضع الملفات في المجلد الرئيسي

---

## 📝 ملاحظات مهمة

1. **HTTPS مطلوب**: Service Workers تعمل فقط على HTTPS (أو localhost)
2. **التحديثات**: Service Worker يتحدث تلقائياً عند تغيير الملف
3. **Cache**: قد تحتاج إلى مسح Cache يدوياً في بعض الأحيان
4. **التثبيت**: قد لا يظهر إشعار التثبيت في أول مرة

---

## ✅ الخلاصة

بعد تنفيذ هذه الخطوات:
- ✅ Service Worker يعمل
- ✅ Manifest صحيح
- ✅ الأيقونات موجودة
- ✅ وضع Offline يعمل جزئياً
- ✅ التطبيق جاهز للتثبيت

إذا واجهت أي مشكلة، راجع قسم "حل المشاكل الشائعة" أعلاه.

