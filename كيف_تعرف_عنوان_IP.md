# 🔍 كيف تعرف عنوان IP لجهازك

## 🎯 الطريقة الأسهل في Windows

---

## 📋 الطريقة 1: PowerShell (موصى به) ⭐

### الخطوات:

#### 1. افتح PowerShell:
```
1. اضغط على زر Windows (⊞)
2. اكتب: PowerShell
3. اضغط Enter
```

أو:
```
1. اضغط Windows + X
2. اختر: Windows PowerShell
```

#### 2. اكتب الأمر:
```powershell
ipconfig
```

#### 3. ابحث عن:
ابحث عن سطر يحتوي على:
```
IPv4 Address. . . . . . . . . . . : 192.168.1.XXX
```

**هذا هو عنوان IP الذي تحتاجه!**

---

## 📋 الطريقة 2: Command Prompt (CMD)

### الخطوات:

#### 1. افتح CMD:
```
1. اضغط على زر Windows (⊞)
2. اكتب: cmd
3. اضغط Enter
```

أو:
```
1. اضغط Windows + R
2. اكتب: cmd
3. اضغط Enter
```

#### 2. اكتب الأمر:
```cmd
ipconfig
```

#### 3. ابحث عن:
```
IPv4 Address. . . . . . . . . . . : 192.168.1.XXX
```

---

## 📋 الطريقة 3: من Settings (الأسهل بدون أوامر)

### الخطوات:

#### 1. افتح Settings:
```
1. اضغط Windows + I
2. أو اكتب "Settings" في قائمة ابدأ
```

#### 2. اذهب إلى Network:
```
Settings → Network & Internet → Wi-Fi
```

#### 3. اضغط على اسم الشبكة:
```
اضغط على اسم Wi-Fi المتصل به
```

#### 4. ابحث عن:
ستجد:
```
IPv4 address: 192.168.1.XXX
```

---

## 🎯 مثال على النتيجة:

عندما تكتب `ipconfig`، سترى شيئاً مثل:

```
Windows IP Configuration

Wireless LAN adapter Wi-Fi:

   Connection-specific DNS Suffix  . :
   IPv4 Address. . . . . . . . . . . : 192.168.1.105  ← هذا هو!
   Subnet Mask . . . . . . . . . . . : 255.255.255.0
   Default Gateway . . . . . . . . . : 192.168.1.1
```

**عنوان IP هو:** `192.168.1.105` (في هذا المثال)

---

## 📱 كيف تستخدمه؟

### بعد معرفة عنوان IP:

#### 1. شغّل الخادم:
```bash
python -m http.server 8000
```

#### 2. افتح من الجوال:
```
http://192.168.1.105:8000
```

(استبدل `192.168.1.105` بعنوان IP الخاص بك)

---

## ⚠️ ملاحظات مهمة:

### 1. عنوان IP قد يتغير:
- إذا أعدت تشغيل الراوتر
- إذا أعدت الاتصال بالشبكة
- إذا غيرت الشبكة

### 2. تأكد من:
- ✅ الجوال والكمبيوتر على نفس Wi-Fi
- ✅ الخادم يعمل (python -m http.server 8000)
- ✅ الجدار الناري (Firewall) يسمح بالاتصال

---

## 🔧 حل المشاكل:

### إذا لم تجد "IPv4 Address":

**جرب:**
```powershell
ipconfig /all
```

هذا يعرض جميع التفاصيل.

---

### إذا كان عنوان IP يبدأ بـ 169.254:

هذا يعني أن الكمبيوتر لا يستطيع الاتصال بالشبكة:
1. تأكد من أن Wi-Fi متصل
2. أعد الاتصال بالشبكة
3. جرب إعادة تشغيل الراوتر

---

### إذا كان عنوان IP يبدأ بـ 127.0.0.1:

هذا هو localhost (للجهاز نفسه فقط):
- ✅ استخدمه للاختبار على الكمبيوتر: `http://localhost:8000`
- ❌ لا يعمل من الجوال

---

## ✅ الخطوات السريعة (ملخص):

```
1. اضغط Windows + X
2. اختر: Windows PowerShell
3. اكتب: ipconfig
4. ابحث عن: IPv4 Address
5. استخدم العنوان مع: http://IP:8000
```

---

## 🎯 مثال كامل:

```powershell
# 1. افتح PowerShell
# 2. اكتب:
ipconfig

# 3. ستجد:
IPv4 Address. . . . . . . . . . . : 192.168.1.105

# 4. شغّل الخادم:
python -m http.server 8000

# 5. افتح من الجوال:
http://192.168.1.105:8000
```

---

**الآن جرب وأخبرني بعنوان IP!** 🔍

