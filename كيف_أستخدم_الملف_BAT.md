# 🚀 كيفية استخدام ملف رفع_GitHub.bat

## ✅ الطريقة الأسهل:

### 1. افتح مجلد `EduActive`

### 2. اضغط مرتين على ملف:
```
رفع_GitHub.bat
```

### 3. سينفذ الأوامر تلقائياً! ✅

---

## ⚠️ عند طلب كلمة المرور:

### سيطلب منك:
- **اسم المستخدم:** `Marwan887-rgb`
- **كلمة المرور:** استخدم **Personal Access Token**

### إنشاء Token:

1. اذهب إلى: https://github.com/settings/tokens
2. اضغط **"Generate new token"** → **"Generate new token (classic)"**
3. **Note:** اكتب أي اسم (مثل: "EduActive")
4. **Select scopes:** حدد **`repo`** ✅
5. اضغط **"Generate token"**
6. **انسخ التوكن** (سيظهر مرة واحدة فقط!)
7. الصقه ككلمة مرور عند الطلب

---

## ✅ بعد التنفيذ:

افتح:
```
https://github.com/Marwan887-rgb/NewEdu
```

يجب أن ترى جميع الملفات! ✅

---

## 🔄 للتحديثات المستقبلية:

بعد أي تعديل، شغّل الملف مرة أخرى:
```
رفع_GitHub.bat
```

أو استخدم الأوامر يدوياً:
```bash
"C:\Program Files\Git\bin\git.exe" add .
"C:\Program Files\Git\bin\git.exe" commit -m "وصف التعديل"
"C:\Program Files\Git\bin\git.exe" push
```

---

**🚀 جاهز! فقط اضغط مرتين على الملف!**

